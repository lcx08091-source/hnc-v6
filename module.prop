id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc32
versionCode=570132
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc32 · 全局带宽整形器(opt-in,默认关):设置页新增"全局带宽整形"——手动填 WAN 上/下行带宽,把整个热点 HTB 父类 1:1 的 ceil 压到 WAN 带宽 + 默认类挂最优 AQM(cake→fq_codel→sfq),让总流量受 WAN 瓶颈管,AQM 才压得住 bufferbloat。默认关→不用的人零风险;诚实标注固定整形对蜂窝收益有限、稳定链路才是主战场。新后端 action global_shaper_set + tc_manager.sh set_global_shaper + restore 恢复 + /api/config 暴露状态。含 Go 改动,需 CI 重编。sh -n + node --check + go vet + android/arm64 交叉编译均通过。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
