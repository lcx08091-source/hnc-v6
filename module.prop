id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc17
versionCode=570117
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc17 · 收尾根因 A 的最后一处 /system/bin 依赖:hnc_watchdog(Go 主守护)的核心保活循环本来就是纯 Go、免疫,但它顺带跑的业务动作(runAction→watchdog.sh)和定时任务(stats_sample/v6_sync/ndpi)用的是硬编码 /system/bin/sh ×6,/system/bin 被卸的几秒里这些杂活会跳过(核心抓包不受影响、过会儿自恢复)。本版加 shellPath() 帮手:有 /system/bin/sh 就用、没有就退到 rc13 预置的 /data mksh 副本(脚本是 mksh 方言,兜底必须用 mksh 不能用 busybox ash),六处调用全改走它。已确认 dpid_supervisor(Go 兜底)与 dpid 核心本就无 /system/bin 依赖;dpid 的 pm/dumpsys/cmd/app_process 是框架工具、无 /data 替代、本就优雅降级。Go 改动需 CI 重编 hnc_watchdog;go vet + 安卓 arm64 交叉编译通过。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
