id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc1
versionCode=570101
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc1 · 本轮 (m2→rc1): 底栏精简为 4 tab (设备/应用/分析/设置), 日志并入设置, 统计+DPI 合并为"分析"段控; 应用 tab 改走 ksu.exec 桥接 (本机现可见 per-uid 字节/SNI); 真实系统应用名 (app_process + 极小 dex 调 PackageManager.getApplicationLabel, 覆盖用户自装/系统/分包 APK, 失败自动回退 curated); 远程面板未配对时自动跳配对页 (修 401 卡死); 新增"自动识别飞轮"(走法2: 以内核 uid 为准, 全新域名按 uid 独占性+持续性自动归类到对应 app, 影子模式默认开仅统计不写规则, touch auto_promote.enabled 放开, 多 uid 矛盾自动降级, 只标注不动作). 详见 CHANGELOG.md 与应用内更新日志.
updateJson=
minKernelSU=11485
