id=hotspot_network_control
name=Hotspot Network Control
version=v5.3.0-rc30.12.18
versionCode=530138
author=Claude (Anthropic AI) + Ling
description=Android 热点带宽/延迟/黑白名单管理 · v5.3.0-rc30.12.18 · 默认拒绝中间件重构: (1) isPublicPath 白名单 + 其他全要 cookie, 替代之前的"敏感读白名单 + 其他放行"模式. (2) auth_required=false 不再有"放行"语义, 任何路径只要不在 isPublicPath 都强制 cookie. (3) service.sh 自动检测老 rules.json auth_required=false 自动升级到 true (UI 一致性). (4) rules.json 默认值改成 true. 行为变化: 升级后必须走一次 /pair 配对. 副作用: 以后加新 API 默认就是 401, 不会再漏鉴权.
minKernelSU=11485
