id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc23
versionCode=570123
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc23 · 继续清"摆着但没接数据"的假显示(设备卡+应用页扫一遍的结果)。设置页"运行状态"面板原来整块是硬编码假值(hotspotd PID 18432 / RES 4.2MB、tc 规则数 11、iptables 7 chains、watchdog 运行中)——没有任何 JS/接口喂数据。改为真实:进设置页时 kexec 实采 hotspotd pid+RSS、tc 对象数、iptables MARK 0x800000 规则数、watchdog 存活,拿不到显示 — 而不是假数字(远程无 kexec 显示"远程不可读")。"热点接口"行接真实 state.hotspotIface(不再写死 wlan2);删掉无真实来源的假"上联接口 rmnet0"行。另核实:开机自动开热点(hotspot_auto)整条链路真实可用(开关→hotspot_save 写 rules.json→开机 service.sh 读取→hotspot_autostart.sh 用 cmd wifi start-softap / cmd tethering / svc 真启 AP + 补 SELinux;开关回读已接 cfg.hotspot_autostart);能否成功取决于 ColorOS 是否放行这些命令,需真机验证。纯前端,不用重编。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
