id=hotspot_network_control
name=Hotspot Network Control
version=v5.3.0-rc30.12.28-hf2
versionCode=530148
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.3.0-rc30.12.28-hf2 · 修复 KSU WebUI auth required 根因 (GPT 三审发现): (1) __hncLoadLocalAdminSecret 回调签名错: ksu.exec 回调实际是 (exitCode, stdout, stderr) 三参数, 之前写 function(out) 拿到的 out 是 exitCode=0, 导致 secret 被设为字符串 "0", 注入 header 后 64 字节校验失败永远 401. (2) KSU WebUI 强制 bridge-first, 跳过 fetch (避免 Origin/CORS 干扰中间件的 loopback 信任分支). (3) KSU 本地 WebUI 收 401 不跳 /pair (那是远程浏览器流程, 本地应走 root secret).
minKernelSU=11485
