id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc31
versionCode=570131
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc31 · 远程 WebUI 功能对齐(daemon/hnc_httpd/web/app.js):① 设备卡片现在直接显示"在用"的 app(dpid 经 /api/devices 的 dpi_apps 上报),和本机 WebUI 折叠卡一致,不再藏二级页;② 每设备"低延迟(智能队列)"开关补到远程——按钮 + 蓝色"低延迟"角标,走后端 rule_sqm,与延迟注入互斥、tc_htb=false 时禁用(对齐本机 capBool 门控)。web 资源 go:embed 进二进制,本版需 CI 重编 hnc_httpd。node --check + go vet + android/arm64 交叉编译均通过。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
