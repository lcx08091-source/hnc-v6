id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc11
versionCode=570111
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc11 · 修"开机必须点重新绑定/抓不到包"(真机日志确诊): 根因是 ColorOS/SukiSU 运行期间间歇性把 /system/bin/* 从模块脚本命名空间移除 + 双监管互杀 dpid。① dpi_rebind.sh 不再启动"竞争的 shell guard"(以前每点一次重新绑定就和 watchdog 的 C launcher 打架、越点越坏)→ 改成保留 C launcher、清崩溃标志、只重启 dpid 子进程。② C launcher 不再把 dpid 被外部 SIGTERM 后的 rc=0 当"人工停止"而放弃监管。③ service.sh 用 C launcher 时清理残留 shell guard。④ shell guard get_iface 取空(工具 ENOENT)不再误判 iface 变更而抖动。dpid 本身没问题。⚠ 需 CI 重编 hnc_launcher。建议用户在 SukiSU 管理器查挂载命名空间设置。rc1~rc10 详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
