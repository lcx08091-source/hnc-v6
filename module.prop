id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc22
versionCode=570122
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc22 · 修两处真机反馈。① 每设备「低延迟」开关点了报 TC_ACTION_BUSY=1:根因是点的瞬间 tc 锁被 watchdog 的 restore/health 短暂占着,而 set_sqm 一抢不到就立刻失败(set_limit/set_delay 共用同锁但你在空窗期点的所以成功)。修复:set_sqm 获取 tc 锁改为短重试(~3s,期间 tc_action_lock 也会自动回收 >25s 的陈旧锁),骑过短暂占用。② 设备卡片"在用 app"一直空、像假的:dpiLine 早就渲染在折叠卡片头部(IP/MAC 下方),但前端设备模型从没把 /api/devices 的 dpi_apps 映射进去 → d.dpi_apps 恒空 → 永不显示。修复:模型补 dpi_apps 映射,dpid 识别到该客户端流量后,折叠卡上直接显示"在用 [app]"(无需展开)。纯 shell+前端,不用重编。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
