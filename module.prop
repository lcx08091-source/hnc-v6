id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc20
versionCode=570120
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc20 · 新功能:每设备「低延迟」开关(直接做到设备卡片里,取代设置页那个复杂又难用的全局 SQM)。展开任一设备卡 → 「低延迟」一键开关 → 把该设备 class 的队列叶子换成 CAKE/fq_codel(AQM),它自己跑满时也跟手。配合限速效果最好(限速=瓶颈,队列排在我们能管的地方);与延迟/弱网注入互斥(同一叶子);开机/切热点 restore 时自动恢复。链路:卡片开关→rule_sqm(Go action_dev_sqm.go)→apply_device_rule alloc_mid + tc_manager.sh set_sqm(新 device_sqm_leaf/set_sqm)+ rules.json per-device sqm_enabled;server.go 把 sqm_enabled 透传到 /api/devices;restore_rules 解析并恢复。Go 改动需 CI 重编 hnc_httpd;go vet + arm64 编译 + node --check + sh -n 均通过。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
