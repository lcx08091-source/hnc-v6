id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc15
versionCode=570115
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc15 · 加固"无挂载模块"对根因 A 的韧性(咨询外部 AI 后取其安全子集):① 本模块本就不改 /system(无 system/ 文件夹),新增空 skip_mount 显式声明"不参与 systemless 挂载"。② provision_fallback_tools 优先用常驻 /data 且 applet 更全(含 awk)的 KSU/Magisk busybox 作 multicall 源(toybox 兜底),并按二进制实际支持的 applet 安全建链(awk/rm/mkdir/mv/stat/dirname... 扩展,只建支持的、不建死链)。③ 修 device_detect.sh(常驻 daemon 循环)PATH 漏掉 /data/local/hnc/bin 的缺口。④ 不切 busybox ash 解释器——脚本是 mksh 方言([[ ]]/(( )))ash 跑不了,继续用 mksh。根治路线仍是用户侧切 Magic Mount 或精准 umount。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
