id=hotspot_network_control
name=Hotspot Network Control
version=v5.7.0-rc35
versionCode=570135
author=Claude (Anthropic AI) + Ling + GPT (audit)
description=Android 热点带宽/延迟/黑白名单管理 · v5.7.0-rc35 · VPN 飞轮排除可视化 + 可管理 + 加单测。① 应用卡片标注:被排除的 VPN/代理应用现在显示"🛡️ VPN/代理·不学习规则"徽标(dpid SelfApp 新增 flywheel_excluded,含内置清单/用户清单/导管自动识别三种来源);未排除的应用卡片上有"排除"按钮一键加入。② WebUI 管理:设置页新增"飞轮排除名单"卡片,可看/增/删你自定义的排除包名(走新 action flywheel_exclude_set 写 etc/flywheel_exclude.json,/api/config 暴露用户清单),不用再手编 JSON。③ 补 Go 单元测试:覆盖排除清单加载(内置常驻/用户合并/坏文件降级)、UID/包名排除判定、构造器播种、候选分级 classifyTier。含 dpid + hnc_httpd(Go)改动,需 CI 重编。go vet + go test + android/arm64 交叉编译 + node --check 全通过。详见 CHANGELOG.md。
updateJson=
minKernelSU=11485
